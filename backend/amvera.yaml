meta:
  environment: node
  name: npm
  version: 20

requirements:
  - package.json

steps:
  - npm install --prefix ..
  - npm run build:shared --prefix ..
  - npm run build:backend --prefix ..
  - npm install

run:
  script: npm start
